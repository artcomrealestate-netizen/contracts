{
  "name": "firestore-rules-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Firestore Security Rules tests for qouta_calculator, run against the Firebase Emulator (see docs/Contract_System_TDD_v1.1_EN.md §55/§64).",
  "scripts": {
    "test": "mocha --timeout 20000 users.rules.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "mocha": "^10.7.3"
  }
}
